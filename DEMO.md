# Demo notes

The project is a static site and can be previewed locally without a build step:

```bash
python -m http.server 8000
```

Open <http://localhost:8000> after starting the server.

The hosted demo is available at <https://00mohammad.github.io/Car-shop/>.

The design references are stored in `assets/Design/`. They are reference images, not runtime dependencies.
